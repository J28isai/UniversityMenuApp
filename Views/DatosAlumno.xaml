<UserControl x:Class="UniversityMenuApp.Views.DatosAlumno"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UniversityMenuApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid ItemsSource="{Binding AlumnoNotas}"
            Margin="33,143,62,44"
            AutoGenerateColumns="False"
            SelectedItem="{Binding Seleccionado}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="IDAlumno" Binding="{Binding Id}" Width="*"/>
                <DataGridTextColumn Header="Nombre del Alumno" Binding="{Binding FullName}" Width="*"/>
                <DataGridTextColumn Header="Id Asignatura" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Nombre De la asignatura" Binding="{Binding Id}" Width="*"/>
                <DataGridTextColumn Header="Nota" Binding="{Binding FullName}" Width="*"/>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
